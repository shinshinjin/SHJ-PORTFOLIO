# SHJ-PORTFOLIO

신혁진의 정적 HTML/CSS/JavaScript 포트폴리오. 빌드 과정 없이 GitHub Pages에서 서비스합니다.

로컬 미리보기: 저장소의 상위 폴더에서 `python3 -m http.server 8000` 실행 후 `http://localhost:8000/SHJ-PORTFOLIO/` 접속.

배포: Settings → Pages → Deploy from a branch → `main` / `/(root)`.
